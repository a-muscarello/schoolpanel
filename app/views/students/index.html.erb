
<h1>This is Dash Academy</h1>

<% @student.each do |student| %>
    <div>
        <h3><%= student.first_name %></h3>
        <h3><%= student.last_name %></h3>
        <h3><%= student.age %></h3>
        <h3><%= student.highest_education %></h3>
    </div>
<% end %>


<br>
# <%= render 'application/header', locals: {student: @studentdeg } %>